// Export tabs if needed, or leave empty if using direct imports in App.tsx
// Currently App.tsx imports directly from ./tabs/...
// So this file can be empty or deleted. Keeping it empty for now to avoid module resolution errors if anything else imports folder.
export { };
